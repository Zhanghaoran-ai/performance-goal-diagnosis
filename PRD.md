# 绩效目标拆解诊断 Skill - 产品需求文档（PRD）

**版本**：v2.3
**最后更新**：2026-08-11
**负责人**：示例员工C

---

## 一、产品概述

### 1.1 产品定位

绩效目标拆解诊断 Skill 是一款面向企业管理者和 HR 的自动化诊断工具，能够自动完成从一号位到管理者到员工的目标逐层拆解可视化，并对目标拆解合理性进行多维度诊断，输出可落地的优化建议和待澄清动作到具体责任人。

### 1.2 核心价值

| 价值维度 | 说明 |
|---------|------|
| **效率提升** | 人工诊断一个团队的 OKR 拆解需要 2-4 小时，自动化诊断仅需 30 秒 |
| **一致性** | 基于统一的诊断标准，避免不同管理者判断标准不一致 |
| **可量化** | 健康度评分 + 八大维度问题统计，让目标拆解质量可衡量 |
| **可落地** | 每个问题都附带具体责任人、优化建议和优先级 |
| **行业对标** | 内置 9 大行业 200+ 指标基准，自动识别行业并智能匹配 |

### 1.3 目标用户

| 用户角色 | 使用场景 | 核心需求 |
|---------|---------|---------|
| **团队管理者** | 季度初 OKR review、季度中进度复盘 | 快速发现团队目标拆解问题，拿到优化建议 |
| **HRBP** | 组织健康度巡检、目标对齐辅导 | 批量诊断多个团队，识别共性问题 |
| **新管理者** | 刚接手团队，了解目标拆解现状 | 快速上手，知道从哪里入手优化 |
| **员工** | 个人目标对齐检查 | 确认自己的目标是否对齐上级方向 |

---

## 二、功能架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      统一入口 diagnose.py                     │
│         数据采集 → 诊断引擎 → 多格式输出（JSON/MD/HTML）      │
└──────────────────────────┬──────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼───────┐  ┌───────▼───────┐  ┌───────▼───────┐
│  数据采集层   │  │  诊断引擎层   │  │  输出展示层   │
│  飞书通讯录   │  │  八大诊断维度 │  │  JSON 结构化  │
│  飞书 OKR     │  │  13种检测模式 │  │  Markdown     │
│  本地JSON     │  │  语义分析引擎 │  │  HTML 可视化  │
│  数据缓存     │  │  行业匹配引擎 │  │  纯文本报告   │
└───────────────┘  └───────────────┘  └───────────────┘
```

### 2.2 核心模块

| 模块 | 文件 | 版本 | 说明 |
|------|------|------|------|
| 统一入口 | `diagnose.py` | v2.2 | 数据采集 + 诊断 + 输出一站式 |
| 诊断核心 | `diagnose_goals.py` | v2.2 | 八大诊断维度 + 健康度评分 + 建议生成 |
| 语义分析 | `semantic_utils.py` | v2.0 | jieba分词 + 同义词词典 + 多维度相似度 |
| **行业匹配引擎** | **`industry_matcher.py`** | **v2.3** | **行业识别 + 分层匹配 + 指标类型 + 质量评估** |
| 可选插件 | `plugins/` | v2.0 | 词向量 / LLM / 主动学习反馈 |

---

## 三、核心功能需求

### 3.1 数据采集（v1.3）

#### 功能描述
支持从飞书通讯录和 OKR 自动采集目标数据，也支持本地 JSON 文件输入。

#### 输入方式（四选一）

| 方式 | 参数 | 说明 | 准确度 |
|------|------|------|--------|
| 部门 ID | `--department-id` | 通过 open_department_id 精确获取 | ⭐⭐⭐⭐⭐ |
| 部门名称 | `--department-name` | 自动搜索匹配部门 | ⭐⭐⭐⭐ |
| 用户列表 | `--user-ids` | 指定用户 ID 列表 | ⭐⭐⭐⭐⭐ |
| 我的团队 | `--my-team` | 获取当前用户的直接下属 | ⭐⭐⭐ |
| 本地文件 | `--input` | 已有 JSON 数据文件 | ⭐⭐⭐⭐⭐ |

#### 三级权限降级

| 权限级别 | 能拿到的数据 | 诊断质量 |
|---------|-------------|---------|
| Level 1 完整权限 | 部门列表 + 汇报线 + 所有人OKR | 完整诊断 |
| Level 2 部分权限 | 部分人员OKR + 部分汇报线 | 基础诊断 + 提示补全 |
| Level 3 最低权限 | 只能看到自己的OKR | 个人诊断 + 引导补充 |

#### 数据缓存
- 默认缓存 24 小时
- 支持 `--use-cache` / `--force-refresh`
- 按部门/用户生成不同缓存键

---

### 3.2 八大诊断维度（v1.0 ~ v1.5）

| 序号 | 维度 | 严重程度 | 检测逻辑 | 版本 |
|------|------|---------|---------|------|
| 1 | 上下层数据不匹配 | 高/中 | 下级目标数值加总 ≠ 上级目标数值 | v1.0 |
| 2 | 无具体执行人 | 高 | 目标未明确负责人或负责人为集体 | v1.0 |
| 3 | 目标重复 | 高/中 | 同一层级存在内容高度相似的目标 | v1.0 |
| 4 | 责任不清 | 高 | 目标边界模糊，多人交叉或真空 | v1.0 |
| 5 | 未向上对齐 | 高/中 | 下级目标未对齐到任何上级目标 | v1.0 |
| 6 | 层次不准确 | 高/中 | 目标粒度过粗/过细，应上移或下移 | v1.0 |
| 7 | **指标偏离行业基准** | 高/中/低 | 目标指标偏离行业基准值 | **v2.2 重构** |
| 8 | 指标关联性与承接 | 高/中/低 | 上下级指标语义关联性、因果关系等 | v1.5 + v2.0 |

---

### 3.3 13 种自动检测模式（v2.0）

**所属维度**：指标关联性与承接

| 序号 | 检测模式 | 说明 | 版本 |
|------|---------|------|------|
| 1 | 关键词关联性检测 | 基于语义相似度判断上下级指标是否相关 | v1.5 |
| 2 | 假拆解风险识别 | 识别高度相似的上下级指标（换汤不换药） | v1.5 |
| 3 | 子指标数量合理性 | 检查子指标数量是否在 2-7 个合理范围 | v1.5 |
| 4 | 承接关系强弱比例 | 统计关联性弱的指标占比 | v1.5 |
| 5 | 因果方向检测 | 检测上下级指标是否因果倒置 | v2.0 |
| 6 | 粒度一致性检测 | 检测同一层级指标粒度是否均匀 | v2.0 |
| 7 | 维度完整性检测 | 基于平衡计分卡等框架检查维度覆盖 | v2.0 |
| 8 | 逻辑闭环检测 | 检查输入-过程-输出是否形成完整闭环 | v2.0 |
| 9 | 命名质量检测 | 检查指标命名是否规范、可衡量 | v2.0 |
| 10 | 子指标重叠检测 | 检测子指标之间是否存在语义重叠 | v2.0 |
| 11 | 数学关系检测 | 识别加总、乘积等数学关系 | v2.0 |
| 12 | 拆解深度检测 | 检查整体拆解深度和各层分布是否合理 | v2.0 |
| 13 | 横向目标对齐检测 | 检测同层级目标之间是否有冲突或重叠 | v2.0 |

---

### 3.4 语义分析引擎（v2.0）

#### 核心特性

| 特性 | 说明 | 效果 |
|------|------|------|
| jieba 中文分词 | 专业中文分词，比简单 n-gram 更准确 | 分词准确度提升 40% |
| HR/绩效领域同义词词典 | 200+ 词对，覆盖收入、用户、效率、质量、HR、客户成功等领域 | 同义词匹配召回率提升 60% |
| 多维度相似度融合 | 字符相似度（20%）+ 词语相似度（60%）+ 结构相似度（20%） | 相似度判断更准确 |
| TF-IDF 关键词权重 | 核心业务词权重更高 | 重要指标匹配更准 |
| 指标类型分类 | 自动识别财务/客户/内部流程/学习成长等 10 大类型 | 维度完整性检测更准确 |
| 因果方向判断 | 自动识别过程指标 vs 结果指标 | 因果倒置检测 |
| 否定词/反向词处理 | 正确处理反向指标（流失率、成本等） | 避免误判 |

#### 降级机制
- 未安装 jieba 时自动降级到简单 n-gram 分词
- 不影响核心功能使用

---

### 3.5 行业指标智能匹配引擎（v2.2 核心 + v2.3 增强）⭐

> **这是 v2.2/v2.3 的核心升级，解决了原指标库"死、平、盲"的三大问题。**

#### 原版本问题（v2.1 及之前）

| 问题 | 说明 | 影响 |
|------|------|------|
| **死** | 指标库是可选的，不传参数就不加载 | 用户不知道有这个功能，使用率低 |
| **平** | 所有指标存在扁平字典，key 是指标名称，丢失行业维度 | 不同行业的同名指标互相覆盖 |
| **盲** | 只按指标名称匹配，不考虑行业、不区分指标方向 | 成本低于基准也会报"偏离"，误报多 |

#### v2.2 解决方案：五大核心能力

##### 能力1：行业自动识别（IndustryRecognizer）

**识别来源**（多源融合）：
- 部门名称（权重 0.3）：如"制造部"→制造业
- 企业名称（权重 0.4）：如"比亚迪"→汽车行业
- 目标内容（权重 0.3）：提取标题、描述、指标名称中的行业关键词

**识别结果**：
- 返回 Top3 候选行业 + 置信度
- 用户手动指定时直接使用，置信度 1.0
- 无识别结果时默认"通用行业"，置信度 0.3

**内置行业关键词库**：11 个行业，200+ 关键词
- 制造业（20个）、消费品行业（19个）、医疗健康行业（21个）、汽车行业（18个）
- 电子半导体行业（20个）、银行业（20个）、物流行业（19个）、新能源行业（19个）
- SaaS行业（22个）、互联网行业（21个）、通用行业（5个）

**行业别名映射**：30 个常见别名标准化
- 互联网科技/IT/信息技术 → 互联网行业
- 软件服务/企业服务/云服务 → SaaS行业
- 快消/消费/零售 → 消费品行业
- 等等

##### 能力2：分层指标匹配（LayeredMetricMatcher）

**三层匹配逻辑**：
1. **行业筛选**：先按识别出的行业缩小候选范围
2. **层级匹配**：L1→战略层、L2→管理层、L3/L4→执行层，层级匹配加 +0.1 分
3. **名称语义匹配**：Jaccard 字符级相似度，<0.3 跳过

**匹配加成**：
- 行业匹配加成：+0.1
- 层级匹配加成：+0.1
- 综合得分 ≥0.5 才返回结果

**输出**：按 total_score 排序返回 TopK，附带指标类型和匹配质量评估

##### 能力3：指标类型判断（MetricTypeClassifier）

| 类型 | 说明 | 模式数量 | 示例 |
|------|------|---------|------|
| **正向指标** | 越高越好 | ~70个 | 收入、利润、满意度、合格率、留存率、OEE |
| **反向指标** | 越低越好 | ~45个 | 成本、流失率、缺陷率、事故率、周期时间 |
| **区间指标** | 在范围内最好 | ~9个 | 库存周转率、资产负债率、毛利率 |
| unknown | 无法判断 | - | 兜底 |

**判断优先级**：区间指标 > 反向指标 > 正向指标 > unknown

##### 能力4：智能偏离判断

根据指标类型智能判断是否偏离：

| 指标类型 | 偏离条件 | 说明 |
|---------|---------|------|
| 正向指标 | 低于基准 10% 以上 | 高于基准是好事，不算偏离 |
| 反向指标 | 高于基准 10% 以上 | 低于基准是好事，不算偏离 |
| 区间指标 | 超出 min/max 范围 | 在范围内都正常 |
| unknown | 绝对值偏离 10% 以上 | 兼容旧逻辑 |

**严重程度分级**：
- high：偏离 >50%
- medium：偏离 20%~50%
- low：偏离 10%~20%
- none：偏离 ≤10%

##### 能力5：匹配质量评估（MatchQualityEvaluator）

**四维加权评分**：

| 维度 | 权重 | 评分逻辑 |
|------|------|---------|
| 匹配相似度 | 30% | 名称匹配得分 |
| 数据源权威性 | 30% | 5星=1.0, 4星=0.85, 3星=0.7, 2星=0.55, 1星=0.4 |
| 数据时效性 | 20% | ≤6月=1.0, ≤12月=0.85, ≤24月=0.7, 其他=0.5 |
| 验证状态 | 20% | 已验证=1.0, 待验证=0.7, 参考值=0.5 |

**可信度等级**：
- A 级（≥0.85）：高可信度
- B 级（≥0.70）：中等可信度
- C 级（≥0.55）：较低可信度
- D 级（<0.55）：低可信度，建议人工核实

#### v2.3 增强能力

##### 增强1：自定义指标库合并（merge_custom_library）

- 支持合并用户自定义的行业指标库
- 支持单个文件或整个目录
- 支持 .md 和 .json 格式
- 可选择是否覆盖同名指标（override 参数）
- 自定义指标标记 `is_custom=True`，便于区分和管理

##### 增强2：用户反馈自动优化（record_feedback + _apply_feedback）

**反馈类型**：
- `benchmark_wrong`：基准值不对
- `range_wrong`：合理范围不对
- `metric_not_found`：指标未找到
- `other`：其他问题

**自动应用机制**：
- 同一指标 + 同一反馈类型 ≥3 条 → 自动应用到指标库
- 取反馈值的平均值作为新基准/新范围
- 被更新的指标标记 `feedback_updated=True`
- 支持反馈统计和查询

##### 增强3：版本兼容性检查（check_version_compatibility）

- 指标库版本号管理（当前 v2.3.0）
- 支持最低版本要求检查
- 返回支持的功能列表
- 便于后续版本迭代和兼容性管理

#### 内置指标库规模

| 行业 | 战略层 | 管理层 | 执行层 | 合计 |
|------|--------|--------|--------|------|
| 制造业 | 8 | 10 | 8 | 26 |
| 消费品/快消 | 7 | 9 | 7 | 23 |
| 医疗健康 | 7 | 9 | 8 | 24 |
| 汽车行业 | 6 | 8 | 7 | 21 |
| 电子/半导体 | 6 | 8 | 7 | 21 |
| 通用行业 | 6 | 8 | 7 | 21 |
| 银行业 | 6 | 8 | 7 | 21 |
| 物流行业 | 6 | 8 | 7 | 21 |
| 新能源行业 | 6 | 8 | 7 | 21 |
| **合计** | **64** | **84** | **68** | **216+** |

#### 指标库数据质量体系（v2.1）

| 管理文档 | 说明 |
|---------|------|
| 数据质量与元数据标准 | 元数据标准、稳定性分级、验证状态定义 |
| 更新机制与流程 | 更新频率、RACI责任矩阵、触发式更新流程 |
| 检验标准与质量控制 | 数据源权威性五级分级、多源交叉验证、偏差预警 |
| 变更日志与版本管理 | 版本管理规则、完整变更日志 |

---

### 3.6 可选插件系统（v2.0）

**所有插件都是可选的，不启用不影响核心功能。**

| 插件 | 功能 | 依赖 | 启用方式 |
|------|------|------|---------|
| 词向量插件 | 基于预训练词向量计算深层语义相似度 | numpy + 词向量文件 | 提供词向量文件自动启用 |
| LLM 插件 | 使用大模型进行深层语义理解和判断 | openai 库 + API 密钥 | 配置 API 密钥自动启用 |
| 反馈插件 | 收集用户反馈，持续优化诊断结果 | 无 | 默认启用，本地存储 |

---

### 3.7 健康度评分

#### 评分规则

- 基础分：100 分
- 高严重度问题：每个扣 10 分
- 中严重度问题：每个扣 5 分
- 低严重度问题：每个扣 2 分
- 最低分：0 分

#### 等级判定

| 分数 | 等级 | 说明 |
|------|------|------|
| 90~100 | 优秀 🟢 | 目标拆解质量很高 |
| 70~89 | 良好 🔵 | 整体不错，有少量优化空间 |
| 50~69 | 一般 🟡 | 有明显问题，需要改进 |
| 0~49 | 较差 🔴 | 问题较多，建议重点优化 |

---

### 3.8 优化建议输出

#### 建议优先级

| 优先级 | 说明 | 响应时间 |
|--------|------|---------|
| P0 | 紧急重要，立即处理 | 本周内 |
| P1 | 重要，尽快处理 | 两周内 |
| P2 | 一般，排期处理 | 一个月内 |

#### 建议结构
- 建议标题
- 建议详情（具体怎么改）
- 责任人（具体到人）
- 时间要求

---

### 3.9 多格式输出

| 格式 | 参数 | 适用场景 |
|------|------|---------|
| JSON | `--format json` | 程序处理、系统集成 |
| Markdown | `--format markdown` | 直接贴到飞书文档 |
| HTML | `--format html` | 浏览器打开、可视化报告 |
| 纯文本 | `--format text` | 命令行查看 |

#### 输出新增字段（v2.2）

**JSON 输出新增 `industry_info` 字段**：
```json
{
  "industry_info": {
    "specified_industry": "用户指定的行业（如有）",
    "recognized_industry": "自动识别的行业",
    "match_info": {
      "recognized": [["制造业", 1.0], ["新能源行业", 0.86], ...],
      "used": "实际使用的行业",
      "confidence": 1.0
    },
    "library_loaded": true,
    "library_stats": {
      "total_metrics": 204,
      "industries": 12,
      "version": "v2.3.0",
      "custom_metrics": 0
    }
  }
}
```

**文本/Markdown/HTML 报告新增"🏭 行业基准匹配信息"区块**：
- 指定行业 / 自动识别行业（含置信度）
- 其他 Top3 候选行业
- 指标库统计（指标数、行业数、版本）

**行业基准诊断问题展示增强**：
- 指标类型标注（positive/negative/range）
- 完整元数据：数据来源、数据截止时间、验证状态
- 合理范围展示
- 匹配质量等级（A/B/C/D）

---

### 3.10 行业指标库架构与应用逻辑

> 本章节详细说明指标库的**存储架构、数据模型、加载流程、匹配应用、更新机制和反馈闭环**，是 3.5 节"行业匹配引擎"的底层支撑。

#### 3.10.1 指标库存储架构

**物理存储**：以 Markdown 文件为主、JSON 文件为辅，按行业和层级组织目录结构。

```
examples/industry-metrics/
├── README.md                          # 总览文档
├── 数据质量与元数据标准.md             # 元数据规范
├── 更新机制与流程.md                   # 更新流程
├── 检验标准与质量控制.md               # 质量控制
├── 变更日志与版本管理.md               # 版本管理
├── 制造业/
│   ├── 战略层指标.md                   # 8 个指标
│   ├── 管理层指标.md                   # 10 个指标
│   └── 执行层指标.md                   # 8 个指标
├── 消费品行业/（3个文件，23个指标）
├── 医疗健康行业/（3个文件，24个指标）
├── 汽车行业/（3个文件，21个指标）
├── 电子半导体行业/（3个文件，21个指标）
├── 通用行业/（3个文件，21个指标）
├── 银行业/（3个文件，21个指标）
├── 物流行业/（3个文件，21个指标）
├── 新能源行业/（3个文件，21个指标）
├── SaaS行业/                          # 早期版本，待更新为三层结构
├── 互联网行业/                        # 早期版本，待更新
└── 通用/                              # 早期版本，待合并
```

**三层指标体系**：

| 层级 | 别名 | 面向对象 | 更新频率 | 指标数量 | 核心作用 |
|------|------|---------|---------|---------|---------|
| 战略层 | 公司级/厂级 | 高管、厂长 | 月/季 | 5-8个 | 承接战略，对标行业 |
| 管理层 | 部门级/车间级 | 部门经理、车间主任 | 周/月 | 8-12个 | 分解战略，运营管控 |
| 执行层 | 班组级/岗位级 | 班组长、一线员工 | 日/班 | 5-8个 | 落地执行，日常改善 |

**层级支撑关系**：
- 自下而上：执行层指标 → 驱动 → 管理层指标 → 驱动 → 战略层指标
- 自上而下：战略层目标 → 分解为 → 管理层目标 → 分解为 → 执行层目标
- 因果关系，不是简单拆分：下层指标是上层指标的驱动因素

#### 3.10.2 指标数据模型

每个指标在内存中表示为一个字典，包含以下字段：

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| name | string | ✅ | 指标名称 | "OEE（设备综合效率）" |
| industry | string | ✅ | 所属行业 | "制造业" |
| level | string | ✅ | 所属层级 | "战略层" |
| benchmark | float | ✅ | 行业基准值 | 65.0 |
| benchmark_display | string | 否 | 基准值展示（带单位） | "65%" |
| min | float | 否 | 合理范围下限 | 60.0 |
| max | float | 否 | 合理范围上限 | 85.0 |
| range_display | string | 否 | 范围展示 | "60% ~ 85%" |
| unit | string | 否 | 单位 | "%" |
| data_source | string | 否 | 数据来源 | "ISO 22400标准" |
| data_date | string | 否 | 数据截止时间 | "2025-12" |
| source_link | string | 否 | 来源链接 | "https://..." |
| stability | string | 否 | 稳定性分级 | "稳定" |
| verification_status | string | 否 | 验证状态 | "已验证" |
| authority | int | 否 | 数据源权威性（1-5星） | 5 |
| description | string | 否 | 指标说明 | "设备综合效率..." |
| is_custom | bool | 否 | 是否自定义指标（v2.3） | false |
| feedback_updated | bool | 否 | 是否被反馈更新（v2.3） | false |

**Markdown 文件格式**（三层指标体系，推荐格式）：

```markdown
# 制造业 - 战略层指标

## 指标列表

| 序号 | 指标名称 | 基准值 | 合理范围 | 数据来源 | 权威性 |
|------|---------|--------|---------|---------|--------|
| 1 | **OEE（设备综合效率）** | 65% | 60% ~ 85% | ISO 22400标准 | ⭐⭐⭐⭐⭐ |

## 指标详解

### 1. OEE（设备综合效率）
- **基准值**：65%
- **合理范围**：60% ~ 85%
- **数据来源**：ISO 22400标准
- **数据截止时间**：2025-12
- **验证状态**：已验证
- **稳定性**：稳定
- **说明**：设备综合效率，衡量生产设备的有效利用程度
```

#### 3.10.3 指标库加载流程

**加载时机**：`GoalDiagnoser` 初始化时默认自动加载（v2.2 起），无需手动指定路径。

```
GoalDiagnoser.__init__()
    │
    ├─ auto_load_metrics = True（默认）
    │
    ├─ IndustryMetricLibrary() 实例化
    │   │
    │   ├─ 定位指标库目录：脚本位置 + ../examples/industry-metrics/
    │   │
    │   ├─ 递归遍历所有 .md 和 .json 文件
    │   │
    │   ├─ 跳过管理文档（README、数据质量标准等5个）
    │   │
    │   ├─ 从文件路径提取行业名（如 /制造业/战略层指标.md → 行业=制造业）
    │   │
    │   ├─ 从文件名提取层级（战略层/管理层/执行层）
    │   │
    │   └─ 按格式解析每个文件：
    │       ├─ 格式一：三层指标体系（表格+指标详解）→ 推荐
    │       ├─ 格式二：只有表格 → 兼容
    │       └─ 格式三：旧版单指标 Markdown/JSON → 兼容
    │
    ├─ 建立行业索引：{行业名: [指标列表]}
    ├─ 建立层级索引：{层级名: [指标列表]}
    │
    └─ 打印加载结果："✅ 行业指标库已加载：204个指标，12个行业"
```

**加载失败处理**：
- 目录不存在 → 打印警告，指标库为空，不影响其他诊断维度
- 文件解析失败 → 跳过该文件，继续加载其他文件
- 全部加载失败 → 行业基准诊断维度自动跳过

**禁用方式**：`--no-industry-metrics` 参数，或 `auto_load_metrics=False`

#### 3.10.4 指标库应用流程（诊断时）

当执行"指标偏离行业基准"诊断维度时，完整应用流程如下：

```
diagnose_industry_benchmark()
    │
    ├─ Step 1: 行业识别（如果用户未指定）
    │   ├─ IndustryRecognizer.recognize(department, company, goals)
    │   ├─ 部门名权重0.3 + 企业名权重0.4 + 目标内容权重0.3
    │   ├─ 返回 Top3 候选 + 置信度
    │   └─ 取置信度最高的行业作为实际使用行业
    │
    ├─ Step 2: 遍历所有目标的所有指标
    │   └─ 对每个指标执行 Step 3~5
    │
    ├─ Step 3: 分层匹配
    │   ├─ LayeredMetricMatcher.match(indicator_name, goal_level, industry)
    │   ├─ 按行业筛选候选指标
    │   ├─ 按层级筛选（L1→战略层, L2→管理层, L3/L4→执行层）
    │   ├─ 计算名称 Jaccard 相似度（<0.3 跳过）
    │   ├─ 行业匹配加成 +0.1，层级匹配加成 +0.1
    │   ├─ 综合得分 ≥0.5 才返回
    │   └─ 按得分排序返回 TopK（默认 Top1）
    │
    ├─ Step 4: 指标类型判断 + 智能偏离
    │   ├─ MetricTypeClassifier.classify(metric_name)
    │   │   └─ 正向/反向/区间/unknown
    │   ├─ MetricTypeClassifier.judge_deviation(current, benchmark, type, min, max)
    │   │   ├─ 正向指标：低于基准10%以上才算偏离
    │   │   ├─ 反向指标：高于基准10%以上才算偏离
    │   │   ├─ 区间指标：超出min/max才算偏离
    │   │   └─ unknown：绝对值偏离10%以上（兼容旧逻辑）
    │   └─ 输出：is_deviation, severity, direction, deviation_pct, description
    │
    ├─ Step 5: 匹配质量评估
    │   ├─ MatchQualityEvaluator.evaluate(metric, match_similarity)
    │   ├─ 匹配相似度30% + 数据源权威性30% + 时效性20% + 验证状态20%
    │   └─ 输出 A/B/C/D 可信度等级
    │
    ├─ Step 6: 构建诊断问题
    │   ├─ 标题："指标偏离行业基准（positive）"
    │   ├─ 描述：包含偏离度、匹配质量等级
    │   ├─ 证据：当前值、行业基准、合理范围、数据来源、数据截止、验证状态
    │   └─ 建议：根据指标类型给定制化建议
    │
    └─ Step 7: 汇总行业匹配信息到 summary.industry_info
        ├─ specified_industry：用户指定的行业
        ├─ recognized_industry：自动识别的行业
        ├─ match_info：Top3候选 + 置信度
        ├─ library_loaded：是否加载成功
        └─ library_stats：指标数、行业数、版本、自定义指标数
```

#### 3.10.5 指标库更新机制

**定期更新**（v2.1 建立）：

| 更新类型 | 频率 | 时间 | 内容 |
|---------|------|------|------|
| 全量复核 | 每年1次 | 每年1月 | 所有指标基准值重新核验 |
| 季度更新 | 每季度1次 | 4月/7月/10月 | 动态指标更新、新增指标 |
| 触发式更新 | 即时 | 随时 | 用户反馈、重大标准发布、行业重大变化 |

**RACI 责任矩阵**：

| 角色 | 负责(R) | 批准(A) | 咨询(C) | 知情(I) |
|------|--------|--------|--------|--------|
| 指标维护者 | ✅ | - | - | - |
| 行业专家 | - | - | ✅ | - |
| Skill 负责人 | - | ✅ | - | - |
| 使用者 | - | - | - | ✅ |

**版本管理规则**：
- 版本号格式：主版本.次版本.修订号（如 v2.3.0）
- 主版本：架构变更（如指标库格式重构）
- 次版本：新增行业/新增大量指标
- 修订号：单个指标基准值更新

#### 3.10.6 用户反馈闭环（v2.3 新增）

**反馈入口**：诊断报告末尾内置反馈通道，支持"指标不准"或"指标不对"。

**反馈处理流程**：

```
用户提交反馈
    │
    ├─ record_feedback(metric_name, industry, feedback_type, value, comment)
    │   ├─ 生成反馈记录（FB-0001, FB-0002...）
    │   ├─ 状态：pending（待处理）
    │   └─ 存入反馈列表
    │
    ├─ 检查同一指标+同一反馈类型的反馈数量
    │   └─ 如果 ≥3 条 → 触发自动应用
    │
    ├─ _apply_feedback(metric_name, industry, feedback_type)
    │   ├─ 取所有同类反馈值的平均值
    │   ├─ 更新指标库中的 benchmark 或 min/max
    │   ├─ 标记 feedback_updated = True
    │   └─ 反馈状态改为 applied（已应用）
    │
    └─ get_feedback_stats()
        ├─ 总反馈数
        ├─ 按类型统计
        ├─ 按状态统计
        └─ 反馈记录列表
```

**反馈类型**：

| 类型 | 说明 | 自动应用逻辑 |
|------|------|-------------|
| benchmark_wrong | 基准值不对 | ≥3条 → 取平均值更新 benchmark |
| range_wrong | 合理范围不对 | ≥3条 → 取平均值更新 min/max |
| metric_not_found | 指标未找到 | 人工审核后新增指标 |
| other | 其他问题 | 人工审核 |

**设计原则**：
- 3 条阈值避免单条错误反馈污染指标库
- 自动应用只更新数值，不删除指标
- 被反馈更新的指标标记 `feedback_updated=True`，可追溯
- 反馈记录持久化存储，支持统计分析

#### 3.10.7 自定义指标库合并（v2.3 新增）

**使用场景**：企业有自己的内部指标基准，希望和内置指标库合并使用。

**合并流程**：

```
merge_custom_library(custom_path, override=False)
    │
    ├─ 判断 custom_path 是文件还是目录
    │
    ├─ 递归遍历所有 .md 和 .json 文件
    │
    ├─ 按相同的解析逻辑解析自定义指标
    │
    ├─ 对每个自定义指标：
    │   ├─ 检查是否已存在同名同行业指标
    │   ├─ 不存在 → 直接添加，标记 is_custom=True
    │   └─ 已存在 →
    │       ├─ override=True → 覆盖原有指标
    │       └─ override=False → 跳过，计入 skipped
    │
    └─ 返回统计：{merged: N, skipped: M, total_custom: K}
```

**支持格式**：
- 单个 .md 文件（三层指标体系格式或旧版格式）
- 单个 .json 文件
- 整个目录（递归遍历）

**与内置指标的区别**：
- 自定义指标标记 `is_custom=True`
- 统计时单独计数（`custom_metrics` 字段）
- 可单独导出或清除

#### 3.10.8 指标库质量控制体系（v2.1）

**数据源权威性五级分级**：

| 等级 | 说明 | 示例 | 权重 |
|------|------|------|------|
| ⭐⭐⭐⭐⭐ | 国际标准/监管机构 | ISO、IEC、国家能源局 | 1.0 |
| ⭐⭐⭐⭐ | 行业协会/头部咨询 | 中国质量协会、麦肯锡 | 0.85 |
| ⭐⭐⭐ | 上市公司财报/行业报告 | 年报、行业白皮书 | 0.7 |
| ⭐⭐ | 第三方数据平台 | 行业数据库 | 0.55 |
| ⭐ | 企业自报/估算 | 企业内部数据 | 0.4 |

**验证状态**：

| 状态 | 说明 | 权重 |
|------|------|------|
| 已验证 | 多源交叉验证一致 | 1.0 |
| 待验证 | 单一来源，待交叉验证 | 0.7 |
| 参考值 | 估算值，仅供参考 | 0.5 |

**稳定性分级**：

| 等级 | 说明 | 更新频率 |
|------|------|---------|
| 稳定 | 行业通用标准，变化极小 | 年更 |
| 较稳定 | 随行业发展缓慢变化 | 半年更 |
| 动态 | 受市场环境影响大 | 季更 |

**偏差预警机制**：

| 偏离程度 | 颜色 | 说明 |
|---------|------|------|
| <10% | 🟢 绿色 | 正常范围 |
| 10%~20% | 🟡 黄色 | 略有偏离，关注 |
| 20%~50% | 🟠 橙色 | 明显偏离，需分析原因 |
| >50% | 🔴 红色 | 严重偏离，重点关注 |

---

## 四、命令行参数

### 4.1 输入方式（四选一）

| 参数 | 说明 | 示例 |
|------|------|------|
| `--department-id` | 部门 ID（最准确） | `--department-id od-xxxxx` |
| `--department-name` | 部门名称（自动搜索） | `--department-name "客户成功中心"` |
| `--user-ids` | 用户 ID 列表，逗号分隔 | `--user-ids "ou_xxx,ou_yyy"` |
| `--my-team` | 我的团队（降级方案） | `--my-team` |
| `--input, -i` | 已有数据文件（跳过采集） | `--input goal_data.json` |

### 4.2 输出选项

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--output, -o` | 输出文件路径 | `diagnosis_result.json` |
| `--format, -f` | 输出格式：json/text/markdown/html | `json` |

### 4.3 诊断选项

| 参数 | 说明 | 版本 |
|------|------|------|
| `--industry-metrics` | 行业指标库文件夹路径（可选） | v1.2 |
| `--industry` | 手动指定行业（如：制造业、汽车行业），不指定则自动识别 | **v2.2 新增** |
| `--no-industry-metrics` | 禁用行业指标库（不加载内置指标库） | **v2.2 新增** |
| `--severity-threshold` | 只显示指定严重度及以上的问题 | v1.0 |
| `--no-semantic` | 禁用语义分析（回退到简单关键词匹配） | v2.0 |
| `--word-vector-file` | 词向量文件路径（启用词向量插件） | v2.0 |
| `--llm-model` | LLM 模型名称（默认 gpt-3.5-turbo） | v2.0 |

### 4.4 缓存选项

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--use-cache` | 使用缓存数据（如果有） | - |
| `--cache-dir` | 缓存目录 | `./.cache` |
| `--force-refresh` | 强制刷新缓存，忽略已有缓存 | - |
| `--cache-ttl` | 缓存过期时间（秒） | `86400`（24小时） |

---

## 五、典型使用场景

### 场景1：季度初 OKR review
**痛点**：季度初设定 OKR 后，不知道拆解得合不合理。
**方案**：`python3 scripts/diagnose.py --department-name "你的部门" --format html`
**产出**：HTML 可视化报告，列出所有问题和优化建议，直接在 review 会上用。

### 场景2：新管理者上任诊断
**痛点**：刚接手一个团队，不了解目标拆解情况。
**方案**：`python3 scripts/diagnose.py --department-id od-xxxxx --format markdown`
**产出**：Markdown 格式诊断报告，快速了解团队目标拆解健康度。

### 场景3：个人目标对齐检查
**痛点**：设定了个人 OKR，不确定有没有对齐上级方向。
**方案**：`python3 scripts/diagnose.py --user-ids "ou_你的id" --format text`
**产出**：检查目标是否都对齐到上级目标，有没有遗漏。

### 场景4：跨团队项目目标对齐
**痛点**：跨团队项目，多个团队目标之间有没有对齐，责任有没有重叠或真空。
**方案**：`python3 scripts/diagnose.py --user-ids "ou_1,ou_2,ou_3" --format html`
**产出**：识别目标重复、责任不清等问题，促进跨团队对齐。

### 场景5：指标体系优化 + 行业对标
**痛点**：团队指标体系混乱，想知道哪些指标有用，以及和行业基准比怎么样。
**方案**：`python3 scripts/diagnose.py --input team_metrics.json --format html --industry 制造业`
**产出**：13 种维度的指标诊断 + 行业基准对标，识别假拆解、因果倒置、偏离行业基准等问题。

---

## 六、版本历史

### v2.3（当前版本）- 2026-08-11

**重大更新：行业指标智能匹配引擎 + 自定义扩展**

✅ **行业指标智能匹配引擎（v2.2 核心）**：
- 新增 `industry_matcher.py` 核心模块，封装5大能力
- 行业自动识别：多源识别 + Top3候选 + 置信度
- 分层指标匹配：行业→层级→名称语义三层匹配
- 指标类型判断：正向/反向/区间自动识别
- 智能偏离判断：根据指标类型智能判断是否偏离
- 匹配质量评估：A/B/C/D 可信度等级
- 默认自动加载内置指标库

✅ **自定义指标库合并（v2.3 新增）**：
- 支持合并用户自定义的行业指标库
- 支持单个文件或整个目录，.md 和 .json 格式

✅ **用户反馈自动优化（v2.3 新增）**：
- 同一指标同一类型反馈 ≥3 条自动应用到指标库
- 支持反馈统计和查询

✅ **版本兼容性检查（v2.3 新增）**：
- 指标库版本号管理（v2.3.0）
- 支持最低版本要求检查

✅ **新命令行参数**：
- `--industry`：手动指定行业
- `--no-industry-metrics`：禁用行业指标库

✅ **输出增强**：
- JSON 新增 `industry_info` 字段
- 报告新增"行业基准匹配信息"区块
- 行业基准问题展示完整元数据和匹配质量

### v2.2 - 2026-08-11

**核心更新：行业指标智能匹配引擎**（已合并到 v2.3）

### v2.1 - 2026-08

**数据质量提升版**：
- 行业指标库数据质量体系（4个管理文档）
- 所有指标补充元数据（数据来源、截止时间、稳定性、验证状态）
- 用户反馈入口
- 更新机制与版本管理

### v2.0 - 2026-08

**语义增强版**：
- 新增语义分析引擎（jieba + 同义词词典 + 多维度相似度）
- 新增 9 种检测模式（从4种扩展到13种）
- 新增可选插件系统（词向量/LLM/反馈）
- 假拆解和关联性检测准确度大幅提升

### v1.5 - 2026-07

- 新增第八大诊断维度：指标关联性与承接关系
- 新增 4 种自动检测模式
- 新增数据质量检查和智能容错机制

### v1.4 - 2026-07

- 新增统一入口脚本 diagnose.py
- 新增 Markdown / HTML 输出格式
- 新增数据缓存机制

### v1.3 - 2026-07

- 新增自动数据采集模块（飞书通讯录 + OKR）
- 三级权限降级机制
- 4 种输入方式

### v1.2 - 2026-06

- 新增第七大诊断维度：指标偏离行业基准
- 支持行业指标库（互联网 + SaaS + 通用）

### v1.1 - 2026-06

- 支持飞书绩效指标详情 API
- 完善数据格式定义

### v1.0 - 2026-06

- 基础版本：六大诊断维度
- 目标拆解树构建
- 健康度评分
- 优化建议生成

---

## 七、技术依赖

### 核心依赖（必须）
- Python 3.7+
- 标准库即可运行核心功能

### 推荐依赖（建议安装）
- jieba：中文分词（`pip install jieba`）
  - 不安装也能运行，自动降级到简单分词

### 可选插件依赖
- numpy：词向量插件需要
- openai：LLM 插件需要
- 词向量文件：词向量插件需要

### 数据采集依赖
- 飞书 CLI（lark-cli）：用于自动采集飞书数据

---

## 八、后续规划（Roadmap）

### v2.4 计划
- [ ] SaaS 和互联网行业指标库更新为三层结构
- [ ] 合并"通用"和"通用行业"两个重复行业
- [ ] 修复 diagnose_goals.py 中诊断函数重复执行的 Bug
- [ ] 在入口脚本暴露自定义指标库和反馈记录参数

### v2.5 计划
- [ ] 诊断规则可视化配置（无需改代码）
- [ ] 支持导出飞书文档格式
- [ ] 批量诊断多个团队并对比

### v3.0 计划
- [ ] 基于 LLM 的深度业务逻辑诊断
- [ ] 目标拆解建议自动生成（不只是找问题，还给出改法）
- [ ] 历史趋势对比（本季度 vs 上季度）
- [ ] 团队目标拆解健康度排行榜

---

## 九、相关文档

- [SKILL.md](./SKILL.md) - Skill 使用手册
- [指标关联性判定 Checklist](./references/indicator-relationship-checklist.md) - 指标关联性与承接关系的判定标准
- [行业指标库 README](./examples/industry-metrics/README.md) - 行业指标库总览
- [数据质量与元数据标准](./examples/industry-metrics/数据质量与元数据标准.md) - 指标库元数据规范
- [更新机制与流程](./examples/industry-metrics/更新机制与流程.md) - 指标库更新流程
- [检验标准与质量控制](./examples/industry-metrics/检验标准与质量控制.md) - 指标库质量控制
- [变更日志与版本管理](./examples/industry-metrics/变更日志与版本管理.md) - 指标库版本管理
