---
name: performance-goal-diagnosis
description: 绩效目标与指标拆解诊断 Skill，生成指标独立成节点的指标拆解树，诊断向上对齐、向下承接、假拆解、因果倒置、数值不闭合、遗漏与层级不合理。支持标准 JSON、用户提供的飞书绩效 HTTP API 配置、可选飞书自建应用创建、接口字段标准化、行业模板桥接及 JSON/Markdown/HTML 报告；严格区分显式关系、语义推断、用户确认和数据缺口，并提供弱关系确认、拒绝、改选与待核实闭环。
---

# 绩效目标拆解诊断 Skill

> 当前版本：v2.7（2026-09-07）

对团队/个人的 OKR/绩效目标拆解进行自动化诊断，识别拆解不合理的地方，输出可落地的优化建议。

## v2.7：飞书绩效接口与弱关系人工闭环

### 使用飞书绩效 API

必须先取得用户提供的真实接口文档或脱敏样例，确认接口地址、只读权限、鉴权、分页和字段映射。复制 `examples/feishu-performance-api-config.example.json` 后执行：

```bash
python3 scripts/diagnose.py \
  --feishu-performance-config /absolute/path/performance-api.json \
  --api-var department_id=od_xxx \
  --api-var cycle_id=cycle_xxx \
  --format html \
  --output diagnosis.html
```

凭证仅从环境变量读取。不得把 token、App Secret、Authorization 值写进配置、报告、日志或测试。

### 创建自建应用（仅在用户没有现成应用时）

读取 `references/feishu-performance-integration.md`。先按真实接口文档确认只读 scope key，再运行：

```bash
node integrations/feishu-app-creator/scripts/create-app.mjs \
  --config /absolute/path/app-config.json \
  --out /absolute/private/path/feishu-app-credential.json \
  --timeout 600
```

创建应用只解决凭证准备，不代表绩效 API 已开放；权限审批、通讯录范围和租户数据范围仍需满足。

### 回读弱关系人工决策

HTML 报告导出 `indicator-relation-decisions.json` 后：

```bash
python3 scripts/diagnose.py \
  --input standardized-data.json \
  --relation-decisions indicator-relation-decisions.json \
  --format html \
  --output diagnosis-reviewed.html
```

显式关系优先级最高；用户确认独立标记为 `user_confirmed`；拒绝和待核实不再被算法重新连线。


### 核心能力
- ✅ **配置驱动数据采集**：支持标准 JSON；也支持用户提供飞书绩效 API 路径、鉴权环境变量、分页和字段映射后自动采集与标准化。没有真实接口配置时不声称已接通
- ✅ **八大诊断维度**：全面检查目标拆解的合理性
- ✅ **13 种自动检测模式**：v2.0 新增，基于语义分析的深度检测
- ✅ **语义分析引擎**：jieba 分词 + 同义词词典 + 多维度相似度融合
- ✅ **可选插件系统**：词向量、LLM、主动学习反馈（按需启用）
- ✅ **健康度评分**：量化评估目标拆解质量
- ✅ **优化建议**：按优先级（P0/P1/P2）输出可落地的改进建议
- ✅ **行业指标库**：支持自定义行业指标库，对标行业基准（200+指标，9大行业三层体系）
- ✅ **指标库数据质量体系**：v2.1 新增，完整的元数据标准、更新机制、检验标准、版本管理
- ✅ **用户反馈入口**：v2.1 新增，诊断报告中内置指标不准/不对的快捷反馈通道
- ✅ **行业指标智能匹配引擎**：v2.2 新增，自动识别行业 + 分层匹配 + 指标类型判断 + 匹配质量评估
- ✅ **自定义指标库合并**：v2.3 新增，支持合并用户自定义的行业指标库
- ✅ **用户反馈自动优化**：v2.3 新增，同一指标收到3条以上反馈自动应用到指标库
- ✅ **版本兼容性检查**：v2.3 新增，指标库版本管理和兼容性检查
- ✅ **标准输出框架**：6 页面标准化输出（概览/指标树/目标树/诊断/建议/人员），统一 UI 和内容维度
- ✅ **目标树诊断融合**：v2.4 新增，诊断结果和优化建议直接融入目标拆解树节点
- ✅ **行业匹配算法优化**：v2.4 优化，同义词映射+包含关系匹配+跨层级匹配，匹配率大幅提升
- ✅ **独立指标拆解树**：v2.6 新增，指标作为独立节点，展示父子边、向上对齐、向下承接和逐边关系诊断
- ✅ **关系来源四态**：输入显式关系；目标关系约束下的语义推断；用户确认/改选；证据不足或人工拒绝/待核实的数据缺口
- ✅ **弱关系人工闭环**：HTML 中确认保留、拒绝、改选上级、待核实，导出 JSON 后通过 `--relation-decisions` 回读
- ✅ **飞书绩效 API 适配**：用户提供接口与字段契约，支持环境变量鉴权、分页、逐人/全量采集和标准化；凭证不进入报告或 ZIP
- ✅ **飞书自建应用准备**：可选应用创建/增量授权资源；只负责应用与凭证准备，不把它误称为绩效数据接口
- ✅ **指标/目标双视图**：拆解页默认进入指标树，可切换保留的目标树视图
- ✅ **层级强化版目标拆解树**：v2.5 新增，部门分组卡片 + L2蓝/L3紫/L4灰层级色条 + 递进缩进 + 折叠 + 上下关联诊断面板
- ✅ **四层语义关联判断法**：v2.5 新增，词面/语义/因果/数值链路四层判定，识别假拆解、因果倒置、数值不闭合
- ✅ **三级权限降级**：权限不足时自动降级，保证能用
- ✅ **多种输出格式**：JSON / Markdown / HTML 可视化报告
- ✅ **数据缓存**：支持缓存，重复诊断更快
- ✅ **数据质量检查**：自动检测数据完整性，给出补充建议
- ✅ **智能容错机制**：数据不足时基于常识给出诊断结果，标注待验证项

### 八大诊断维度

| 维度 | 说明 | 严重程度 |
|------|------|---------|
| 上下层数据不匹配 | 下级目标数值加总 ≠ 上级目标数值 | 高/中 |
| 无具体执行人 | 目标未明确负责人或负责人为集体 | 高 |
| 目标重复 | 同一层级存在内容高度相似的目标 | 高/中 |
| 责任不清 | 目标边界模糊，多人交叉或真空 | 高 |
| 未向上对齐 | 下级目标未对齐到任何上级目标 | 高/中 |
| 层次不准确 | 目标粒度过粗/过细，应上移或下移 | 高/中 |
| 指标偏离行业基准 | 目标指标偏离行业基准值（需指标库） | 高/中/低 |
| 指标关联性与承接 | 上下级指标语义关联性、因果关系、维度完整性等 | 高/中/低 |

### v2.0 新增：13 种自动检测模式

**指标关联性与承接维度包含 13 种自动检测：**

| 序号 | 检测模式 | 说明 | 版本 |
|------|---------|------|------|
| 1 | 关键词关联性检测 | 基于语义相似度判断上下级指标是否相关 | v1.5 |
| 2 | 假拆解风险识别 | 识别高度相似的上下级指标（换汤不换药） | v1.5 |
| 3 | 子指标数量合理性 | 检查子指标数量是否在 2-7 个合理范围 | v1.5 |
| 4 | 承接关系强弱比例 | 统计关联性弱的指标占比 | v1.5 |
| 5 | 因果方向检测 | 检测上下级指标是否因果倒置 | v2.0 新增 |
| 6 | 粒度一致性检测 | 检测同一层级指标粒度是否均匀 | v2.0 新增 |
| 7 | 维度完整性检测 | 基于平衡计分卡等框架检查维度覆盖 | v2.0 新增 |
| 8 | 逻辑闭环检测 | 检查输入-过程-输出是否形成完整闭环 | v2.0 新增 |
| 9 | 命名质量检测 | 检查指标命名是否规范、可衡量 | v2.0 新增 |
| 10 | 子指标重叠检测 | 检测子指标之间是否存在语义重叠 | v2.0 新增 |
| 11 | 数学关系检测 | 识别加总、乘积等数学关系 | v2.0 新增 |
| 12 | 拆解深度检测 | 检查整体拆解深度和各层分布是否合理 | v2.0 新增 |
| 13 | 横向目标对齐检测 | 检测同层级目标之间是否有冲突或重叠 | v2.0 新增 |

### v2.0 新增：语义分析引擎

**核心特性：**
- 🧠 **jieba 中文分词**：专业中文分词，比简单 n-gram 更准确
- 📚 **HR/绩效领域同义词词典**：200+ 词对，覆盖收入、用户、效率、质量、HR、客户成功等领域
- 🔗 **多维度相似度融合**：字符相似度（20%）+ 词语相似度（60%）+ 结构相似度（20%）
- 📊 **TF-IDF 关键词权重**：核心业务词权重更高
- 🏷️ **指标类型分类**：自动识别财务/客户/内部流程/学习成长等 10 大类型
- ↕️ **因果方向判断**：自动识别过程指标 vs 结果指标
- ❌ **否定词/反向词处理**：正确处理反向指标（流失率、成本等）

### v2.0 新增：可选插件系统

**所有插件都是可选的，不启用不影响核心功能。**

| 插件名称 | 功能说明 | 依赖 | 启用方式 |
|---------|---------|------|---------|
| **词向量插件** | 基于预训练词向量计算深层语义相似度 | numpy + 词向量文件 | 提供词向量文件自动启用 |
| **LLM 插件** | 使用大模型进行深层语义理解和判断 | openai 库 + API 密钥 | 配置 API 密钥自动启用 |
| **反馈插件** | 收集用户反馈，持续优化诊断结果 | 无 | 默认启用，本地存储 |

**插件能力：**
- 词向量插件：深层语义相似度、词嵌入表示
- LLM 插件：深层语义分析、因果关系检测、指标质量评估、行业最佳实践
- 反馈插件：用户反馈收集、准确率统计、优化建议生成

---

## 快速开始

### 5 分钟上手

```bash
# 1. 进入 Skill 目录
cd performance-goal-diagnosis

# 2. 用示例数据跑一遍（推荐先试这个）
python3 scripts/diagnose.py \
  --input examples/end-to-end-demo/sample_input.json \
  --output my_report.html \
  --format html

# 3. 用浏览器打开 my_report.html 查看结果
```

### 用真实数据诊断

```bash
# 方式1：通过部门名称诊断（最常用，自动搜索部门）
python3 scripts/diagnose.py --department-name "你的部门名"

# 方式2：通过部门 ID 诊断（最准确）
python3 scripts/diagnose.py --department-id od-xxxxx

# 方式3：通过用户列表诊断
python3 scripts/diagnose.py --user-ids "ou_xxx,ou_yyy,ou_zzz"

# 方式4：诊断我的团队
python3 scripts/diagnose.py --my-team
```

### 输出格式选择

```bash
# JSON 格式（默认，适合程序处理）
python3 scripts/diagnose.py --input data.json --format json

# Markdown 格式（可直接贴到飞书文档）
python3 scripts/diagnose.py --input data.json --format markdown

# HTML 可视化报告（可直接在浏览器打开）
python3 scripts/diagnose.py --input data.json --format html

# 纯文本格式（命令行查看）
python3 scripts/diagnose.py --input data.json --format text
```

### 启用可选插件

```bash
# 启用 LLM 插件（通过环境变量配置）
export LLM_API_KEY="your-api-key"
export LLM_API_BASE="https://api.openai.com/v1"
python3 scripts/diagnose.py --input data.json --format html

# 启用词向量插件（指定词向量文件）
python3 scripts/diagnose.py --input data.json --word-vector-file ./data/word_vectors.txt
```

---

## 使用方式

### 统一入口（推荐）

使用 `scripts/diagnose.py` 一站式完成数据采集 + 诊断 + 输出：

```bash
python3 scripts/diagnose.py [选项]
```

#### 输入方式（四选一）

| 参数 | 说明 | 示例 |
|------|------|------|
| `--department-id` | 部门 ID（最准确） | `--department-id od-xxxxx` |
| `--department-name` | 部门名称（自动搜索） | `--department-name "客户成功中心"` |
| `--user-ids` | 用户 ID 列表，逗号分隔 | `--user-ids "ou_xxx,ou_yyy"` |
| `--my-team` | 我的团队（降级方案） | `--my-team` |
| `--input, -i` | 已有数据文件（跳过采集） | `--input goal_data.json` |

#### 输出选项

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--output, -o` | 输出文件路径 | `diagnosis_result.json` |
| `--format, -f` | 输出格式：json/text/markdown/html | `json` |

#### 诊断选项

| 参数 | 说明 |
|------|------|
| `--industry-metrics` | 行业指标库文件夹路径（可选） |
| `--import-template` | v2.5 新增：传入 industry-performance-template 技能输出的绩效方案 Markdown，自动转换为行业指标库并完成对标诊断（一键"模板→诊断"闭环） |
| `--tree-style` | 拆解视图样式：enhanced（默认；独立指标树 + 层级强化目标树）/ legacy（旧版目标树） |
| `--severity-threshold` | 只显示指定严重度及以上的问题：high/medium/low（默认 low） |
| `--no-semantic` | 禁用语义分析（回退到简单关键词匹配） |
| `--word-vector-file` | 词向量文件路径（启用词向量插件） |
| `--llm-model` | LLM 模型名称（默认 gpt-3.5-turbo） |

#### 缓存选项

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--use-cache` | 使用缓存数据（如果有） | - |
| `--cache-dir` | 缓存目录 | `./.cache` |
| `--force-refresh` | 强制刷新缓存，忽略已有缓存 | - |
| `--cache-ttl` | 缓存过期时间（秒） | `86400`（24小时） |

#### 其他选项

| 参数 | 说明 |
|------|------|
| `--department-id-type` | 部门 ID 类型：open_department_id/department_id |
| `--verbose, -v` | 详细输出模式 |
| `--validate` | 只校验输入数据，不运行诊断 |

### 分步使用（高级）

如果你想分开执行数据采集和诊断，可以使用两个独立脚本：

```bash
# 第一步：采集数据
python3 scripts/collect_data.py \
  --department-name "团队名" \
  --output goal_data.json

# 第二步：运行诊断
python3 scripts/diagnose_goals.py \
  --input goal_data.json \
  --output diagnosis_result.json \
  --industry-metrics examples/industry-metrics
```

---

## 输入数据格式

### 标准格式

```json
{
  "meta": {
    "collect_time": "2026-08-09 15:30:00",
    "permission_level": 1,
    "total_users": 10,
    "users_with_okr": 8,
    "total_goals": 20
  },
  "goals": [
    {
      "goal_id": "G-0001",
      "title": "目标标题",
      "description": "目标描述（可选）",
      "level": "L1",
      "owner_id": "ou_xxx",
      "owner_name": "张三",
      "department": "部门名",
      "parent_goal_ids": [],
      "aligned_to_goal_ids": [],
      "indicators": [
        {
          "indicator_id": "I-001",
          "name": "指标名",
          "value": 100,
          "unit": "个",
          "parent_indicator_ids": ["I-000"]
        }
      ],
      "progress": 0.5,
      "status": "进行中",
      "weight": 0.25,
      "key_results": [
        {"id": "KR1", "title": "KR 描述", "progress": 0.4}
      ]
    }
  ],
  "users": [
    {
      "user_id": "ou_xxx",
      "name": "张三",
      "position": "职位",
      "department": "部门",
      "manager_id": "ou_yyy",
      "level": "L1",
      "email": "zhangsan@example.com",
      "avatar": ""
    }
  ]
}
```

### 字段说明

#### meta（元信息）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| collect_time | string | 否 | 数据采集时间 |
| permission_level | number | 否 | 权限级别（1=完整，2=部分，3=最低） |
| total_users | number | 否 | 总人数 |
| users_with_okr | number | 否 | 有 OKR 的人数 |
| total_goals | number | 否 | 目标总数 |

#### goals（目标列表）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| goal_id | string | ✅ | 目标唯一 ID |
| title | string | ✅ | 目标标题 |
| description | string | 否 | 目标描述 |
| level | string | ✅ | 层级（L1/L2/L3/L4） |
| owner_id | string | ✅ | 负责人 ID |
| owner_name | string | ✅ | 负责人姓名 |
| department | string | 否 | 所属部门 |
| parent_goal_ids | string[] | 否 | 父目标 ID 列表 |
| aligned_to_goal_ids | string[] | 否 | 对齐的目标 ID 列表 |
| indicators | object[] | 否 | 指标列表 |
| progress | number | 否 | 进度（0-1） |
| status | string | 否 | 状态 |
| weight | number | 否 | 权重（0-1） |
| key_results | object[] | 否 | 关键结果列表 |

#### users（人员列表）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| user_id | string | ✅ | 用户唯一 ID |
| name | string | ✅ | 姓名 |
| position | string | 否 | 职位 |
| department | string | 否 | 部门 |
| manager_id | string | 否 | 上级 ID |
| level | string | 否 | 层级 |
| email | string | 否 | 邮箱 |
| avatar | string | 否 | 头像 URL |

---

## 输出结果说明

### 健康度评分规则

- 基础分：100 分
- 高严重度问题：每个扣 10 分
- 中严重度问题：每个扣 5 分
- 低严重度问题：每个扣 2 分
- 最低分：0 分

**等级判定**：

| 分数 | 等级 | 说明 |
|------|------|------|
| 90~100 | 优秀 🟢 | 目标拆解质量很高 |
| 70~89 | 良好 🔵 | 整体不错，有少量优化空间 |
| 50~69 | 一般 🟡 | 有明显问题，需要改进 |
| 0~49 | 较差 🔴 | 问题较多，建议重点优化 |

### 结果结构

```json
{
  "summary": {
    "total_goals": 20,
    "total_users": 10,
    "total_issues": 5,
    "high_count": 1,
    "medium_count": 2,
    "low_count": 2,
    "health_score": 78,
    "health_level": "良好",
    "by_dimension": {
      "责任不清": 2,
      "未向上对齐": 1,
      "...": "..."
    }
  },
  "issues": [
    {
      "issue_id": "ISSUE-0001",
      "dimension": "责任不清",
      "severity": "high",
      "title": "责任真空",
      "description": "问题描述",
      "evidence": "问题证据",
      "goal_id": "G-0001",
      "goal_title": "目标标题",
      "owner_id": "ou_xxx",
      "suggestion": "改进建议"
    }
  ],
  "suggestions": [
    {
      "suggestion_id": "SUG-001",
      "priority": "P0",
      "title": "建议标题",
      "description": "建议详情",
      "owner": "责任人",
      "deadline": "时间要求"
    }
  ],
  "goal_tree": {},
  "indicator_tree": {
    "version": "2.6",
    "summary": {
      "total_nodes": 0,
      "explicit_edges": 0,
      "inferred_edges": 0,
      "data_gap_count": 0
    },
    "nodes": [],
    "edges": [],
    "data_gaps": []
  },
  "user_summary": [],
  "semantic_info": {
    "semantic_enabled": true,
    "jieba_available": true,
    "synonym_count": 200,
    "detection_modes": 13,
    "plugins_loaded": []
  }
}
```

---

## 标准输出框架

当前采用统一的 6 页面标准化输出框架：概览、指标拆解树、目标拆解树、诊断结果、优化建议、人员视图。指标树是默认拆解视图，目标树作为独立保留视图。

> 详细规范请参考：[标准输出框架规范](references/standard-output-framework.md)

### 整体结构

采用左侧导航 + 主内容区的布局，包含 6 个页面：

| 页面 | 核心内容 |
|------|---------|
| 📊 **概览 Dashboard** | 健康度总览、核心指标卡片、多维度图表 |
| 🌳 **指标拆解树** | 独立指标节点、上下承接、关系来源、四层语义诊断、人工确认与修正 |
| 🎯 **目标拆解树** | 4 层目标体系可视化、节点状态和目标详情 |
| 🔍 **诊断结果** | 八大维度问题清单、严重度统计、详细证据 |
| 💡 **优化建议** | 按责任人分组的行动项、P0/P1/P2 优先级 |
| 👥 **人员视图** | 组织架构树、人员目标和问题分布 |

### 页面 1：概览 Dashboard

**核心指标卡片（6 个）**：
- 整体健康度（环形图 + 分数 + 等级）
- 目标总数
- 平均完成率
- 问题总数（含环比变化）
- 高严重度问题（含环比变化）
- 涉及人员

**图表区（7 个图表）**：
- 问题严重度分布（环形图）
- 目标完成率分布（柱状图）
- 各部门问题数量对比（柱状图）
- 六大维度问题数量（横向条形图）
- 各层级问题分布（堆叠柱状图）
- 问题趋势（近 4 周期折线图）
- TOP5 问题责任人（横向条形图）

### 页面 2：指标拆解树

- 每个指标作为独立节点，显示层级、负责人、所属目标、数值和单位
- 显式关系、语义推断、用户确认和数据缺口采用不同线型与来源标签
- 点击节点查看向上关联、向下承接、四层判断、风险和证据
- 对弱关系支持确认保留、拒绝、改选上级、待核实和 JSON 导出
- 窄屏使用底部详情浮层，操作按钮触控区域不小于 44×44px

### 页面 3：目标拆解树

- 4 层目标体系可视化（L1公司级 → L2部门级 → L3团队级 → L4员工级）
- 节点状态颜色：🟢健康 / 🟣正常 / 🟡警告 / 🔴严重
- 每个节点显示：层级标签、目标标题、负责人、指标进度
- 详情面板：基本信息、关键指标摘要、下级目标列表

### 页面 4：诊断结果

- 严重度统计：🔴高 / 🟡中 / 🟢低
- 六大维度卡片（可点击筛选）：
  - 上下层数据不匹配（橙色）
  - 无具体执行人（黄色）
  - 目标重复（紫色）
  - 责任不清（蓝色）
  - 未向上对齐（青色）
  - 层次不准确（粉色）
- 每个维度的问题列表：严重度、问题描述、偏差率、缺口量、相似度、展开详情

### 页面 5：优化建议

- 优先级统计卡片：P0紧急（红）/ P1重要（黄）/ P2一般（蓝）
- 筛选条件：优先级、维度、责任人
- 按责任人分组的行动清单
- 每条建议：优先级标签、建议内容、关联问题、责任人、截止日期、维度标签

### 页面 6：人员视图

- 人员分类统计：高管 / 总监 / 员工
- 左侧组织架构树（可展开/收起，每人显示问题数量徽章）
- 右侧人员详情面板：基本信息、目标概览、目标列表、关联问题、优化建议

### UI 设计规范

| 元素 | 规范 |
|------|------|
| 侧边栏 | 深蓝/黑色背景，200px 宽 |
| 主内容区 | 浅灰背景 #f5f5f7 |
| 卡片 | 白色背景，12px 圆角，轻微阴影 |
| 状态色 | 健康绿 #10b981 / 正常紫 #8b5cf6 / 警告黄 #f59e0b / 严重红 #ef4444 |
| 优先级 | P0红 #ef4444 / P1黄 #f59e0b / P2蓝 #3b82f6 |

### 使用方式

```bash
# HTML 输出默认采用标准输出框架（6 页面 Tab 切换）
python3 scripts/diagnose.py --input data.json --format html --output report.html

# Markdown 输出按 6 页面分章节
python3 scripts/diagnose.py --input data.json --format markdown --output report.md
```

---

## 行业指标库

### 功能说明

支持关联本地文件夹作为行业指标库，自动对标行业基准，识别偏离过大的指标。

**v1.4.1 重大更新**：新增 9 大行业的**三层指标体系**（战略层/管理层/执行层），总指标数从 5 个扩充到 200+ 个。

### 使用方式

```bash
python3 scripts/diagnose.py \
  --input data.json \
  --industry-metrics examples/industry-metrics \
  --output result.json
```

### 三层指标体系

| 层级 | 别名 | 面向对象 | 更新频率 | 指标数量 | 核心作用 |
|------|------|---------|---------|---------|---------|
| **战略层** | 公司级/厂级 | 高管、厂长 | 月/季 | 5-8个 | 承接战略，对标行业 |
| **管理层** | 部门级/车间级 | 部门经理、车间主任 | 周/月 | 8-12个 | 分解战略，运营管控 |
| **执行层** | 班组级/岗位级 | 班组长、一线员工 | 日/班 | 5-8个 | 落地执行，日常改善 |

**层级支持关系**：
- 自下而上支撑：执行层指标 → 驱动 → 管理层指标 → 驱动 → 战略层指标
- 自上而下分解：战略层目标 → 分解为 → 管理层目标 → 分解为 → 执行层目标
- 因果关系，不是简单拆分：下层指标是上层指标的驱动因素

### 内置行业覆盖

| 序号 | 行业 | 战略层 | 管理层 | 执行层 | 合计 |
|------|------|--------|--------|--------|------|
| 1 | 制造业 | 8 | 10 | 8 | 26 |
| 2 | 消费品/快消 | 7 | 9 | 7 | 23 |
| 3 | 医疗健康 | 7 | 9 | 8 | 24 |
| 4 | 汽车行业 | 6 | 8 | 7 | 21 |
| 5 | 电子/半导体 | 6 | 8 | 7 | 21 |
| 6 | 通用行业 | 6 | 8 | 7 | 21 |
| 7 | 银行业 | 6 | 8 | 7 | 21 |
| 8 | 物流行业 | 6 | 8 | 7 | 21 |
| 9 | 新能源行业 | 6 | 8 | 7 | 21 |
| **合计** | - | **64** | **84** | **68** | **216+** |

### 指标库格式

支持多种格式，自动识别。

#### 格式一：三层指标体系（推荐）

每个行业一个文件夹，包含三个文件：

```
行业名/
├── 战略层指标.md
├── 管理层指标.md
└── 执行层指标.md
```

每个文件包含多个指标，格式如下：

```markdown
# 行业名 - 战略层指标

## 指标列表

| 序号 | 指标名称 | 基准值 | 合理范围 | 数据来源 | 权威性 |
|------|---------|--------|---------|---------|--------|
| 1 | **OEE（设备综合效率）** | 65% | 60% ~ 85% | ISO 22400标准 | ⭐⭐⭐⭐⭐ |

## 指标详解

### 1. OEE（设备综合效率）
- **基准值**：65%
- **合理范围**：60% ~ 85%
- **数据来源**：ISO 22400标准
- **权威性**：⭐⭐⭐⭐⭐
- **说明**：设备综合效率，衡量生产设备的有效利用程度
```

#### 格式二：单指标 Markdown（兼容旧版）

```markdown
# 指标名称

## 基本信息
- **指标名称**：营收增长率
- **行业**：互联网行业
- **单位**：%

## 基准值
- **行业基准**：20
- **合理范围**：15 ~ 30
```

#### 格式三：JSON 格式

```json
{
  "name": "营收增长率",
  "industry": "互联网行业",
  "unit": "%",
  "benchmark": 20,
  "min": 15,
  "max": 30,
  "description": "..."
}
```

### 匹配规则

- 使用 Jaccard 字符相似度匹配指标名称
- 相似度阈值：50%
- 取相似度最高的指标作为匹配结果

### 偏离判定

| 偏离程度 | 触发条件 | 严重度 |
|----------|----------|--------|
| 严重偏离 | 偏离 > 50% | high |
| 明显偏离 | 偏离 20% ~ 50% | medium |
| 略有偏离 | 偏离 10% ~ 20% | low |
| 正常范围 | 偏离 ≤ 10% | - |

### 示例指标库

Skill 自带示例指标库，位于 `examples/industry-metrics/`：

```
examples/industry-metrics/
├── README.md                    # 总览文档
├── 制造业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 消费品行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 医疗健康行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 汽车行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 电子半导体行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 通用行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 银行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 物流行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── 新能源行业/
│   ├── 战略层指标.md
│   ├── 管理层指标.md
│   └── 执行层指标.md
├── SaaS行业/                    # 早期版本
├── 互联网行业/                  # 早期版本
└── 通用/                        # 早期版本
```

---

## 数据质量检查与异常处理

### 功能说明

v1.5 新增数据质量自动检查功能，在诊断开始前自动检测数据完整性，识别缺失的数据类型，并给出补充建议。

### 检查项

| 检查项 | 严重程度 | 说明 |
|--------|---------|------|
| 数据严重不足 | 🔴 高 | 没有找到任何目标数据 |
| 人员数据缺失 | 🟡 中 | 没有人员信息，部分诊断维度可能不准确 |
| 指标数据不足 | 🟡 中 | 少于30%的目标有明确的指标数据 |
| 层级关系不完整 | 🟢 低 | 少于30%的目标有明确的父目标关系 |
| 层级单一 | 🟢 低 | 目标只有一个层级，无法进行上下层拆解诊断 |

### 异常处理机制

当数据不完整时，Skill 不会直接报错退出，而是：

1. **继续诊断**：基于现有数据尽可能完成诊断
2. **明确标注**：在报告开头显示数据质量警告
3. **给出建议**：提示用户需要补充哪些数据
4. **免责声明**：说明结论仅供参考，建议人工复核

### 智能容错机制

对于缺乏数据支撑的场景：
- 只基于现有证据、行业指标库和可复现规则继续诊断，不把常识补全当作输入事实
- 对不确定的结论标注“待验证”或“数据缺口”
- 缺少指标级关系时保留独立节点；只有满足目标关系约束和语义阈值时才生成候选推断边
- 关系错乱时输出问题、证据和补数建议，不强制恢复或伪造父子关系

---

## 指标关联性判定 Checklist

### 概述

v1.5 新增第八大诊断维度——指标关联性与承接关系，从语义层面判断上下级指标之间的关联性和承接关系是否合理。

v2.0 大幅增强：从 4 种检测模式扩展到 13 种，引入语义分析引擎。

详细的判定标准请参考：`references/indicator-relationship-checklist.md`

### 五大检查维度

| 维度 | 分值 | 核心检查点 |
|------|------|-----------|
| 关联性检查 | 30分 | 直接关联性、维度一致性、范围匹配性 |
| 承接关系检查 | 30分 | 因果承接、数学承接、层级承接 |
| 完整性检查 | 20分 | 维度完整性、逻辑闭环 |
| 可执行性检查 | 20分 | 可衡量性、可影响性 |
| **总分** | **100分** | - |

### 常见问题模式

1. **假拆解**：上下级指标只是换了个说法，本质是同一个东西
2. **错位拆解**：有关联但不是直接承接关系，维度错位
3. **遗漏关键维度**：只覆盖部分方面，重要维度缺失
4. **粒度不均**：有的宏观有的微观，不均匀
5. **因果倒置**：把结果当原因，因果方向反了

### 自动检测能力（13 种）

**v1.5 基础检测（4 种）：**
- ✅ 关键词关联性（基于 Jaccard 相似度）
- ✅ 假拆解风险（高度相似的上下级指标）
- ✅ 子指标数量合理性（2-7个为宜）
- ✅ 指标承接关系强弱比例

**v2.0 新增检测（9 种）：**
- ✅ 因果方向检测（过程指标 vs 结果指标）
- ✅ 粒度一致性检测（战略级/管理级/执行级）
- ✅ 维度完整性检测（平衡计分卡/人机料法环/4P等框架）
- ✅ 逻辑闭环检测（输入-过程-输出）
- ✅ 命名质量检测（度量单位/模糊表述）
- ✅ 子指标重叠检测（语义相似度）
- ✅ 数学关系检测（加总/乘积/比例）
- ✅ 拆解深度检测（整体深度和各层分布）
- ✅ 横向目标对齐检测（同层级重叠/冲突）

**需要人工判断：**
- 深层业务逻辑关联性
- 复杂因果关系链
- 行业特定的指标逻辑
- 业务场景的特殊性

---

## 可选插件系统

### 概述

v2.0 新增可选插件系统，提供增强功能。所有插件都是可选的，不启用不影响核心功能。

### 插件列表

#### 1. 词向量插件（WordVectorPlugin）

**功能**：
- 基于预训练中文词向量计算深层语义相似度
- 支持词嵌入表示
- 比关键词匹配更准确的语义理解

**依赖**：
- numpy
- 词向量文件（txt 格式，约几十MB）

**启用方式**：
```bash
python3 scripts/diagnose.py --input data.json --word-vector-file ./data/word_vectors.txt
```

**效果**：语义准确度提升 30-50%

#### 2. LLM 插件（LLMPlugin）

**功能**：
- 深层语义分析
- 因果关系深度检测
- 指标质量专业评估
- 目标对齐检查
- 行业最佳实践建议

**依赖**：
- openai 库
- LLM API 密钥

**启用方式**：
```bash
export LLM_API_KEY="your-api-key"
export LLM_API_BASE="https://api.openai.com/v1"
python3 scripts/diagnose.py --input data.json --llm-model gpt-3.5-turbo
```

**效果**：语义准确度提升 80%+，接近人工判断水平

#### 3. 反馈插件（FeedbackPlugin）

**功能**：
- 收集用户对诊断结果的反馈
- 统计各维度准确率
- 基于反馈生成优化建议
- 本地存储，隐私安全

**依赖**：无（默认启用）

**使用方式**：
```python
from plugins.feedback_plugin import FeedbackPlugin

plugin = FeedbackPlugin()
plugin.initialize()
plugin.add_feedback(
    issue_id="ISSUE-001",
    is_correct=True,
    user_comment="诊断很准确",
    context={"dimension": "指标关联性与承接"}
)
stats = plugin.get_feedback_stats()
suggestions = plugin.get_optimization_suggestions()
```

**效果**：持续优化，越用越准

### 插件架构

```
plugins/
├── __init__.py           # 插件系统入口
├── base_plugin.py        # 插件基类
├── plugin_manager.py     # 插件管理器
├── word_vector_plugin.py # 词向量插件
├── llm_plugin.py         # LLM 插件
└── feedback_plugin.py    # 反馈插件
```

### 开发自定义插件

继承 `BasePlugin` 类，实现 `initialize` 和 `get_capabilities` 方法：

```python
from plugins.base_plugin import BasePlugin

class MyPlugin(BasePlugin):
    def __init__(self):
        super().__init__('MyPlugin', '1.0.0')
    
    def initialize(self, **kwargs):
        # 初始化逻辑
        return True
    
    def get_capabilities(self):
        return ['my_custom_feature']
    
    def my_custom_feature(self, *args, **kwargs):
        # 功能实现
        pass
```

---

## 权限降级机制

### 三级权限体系

| 权限级别 | 名称 | 能拿到的数据 | 诊断质量 |
|---------|------|-------------|---------|
| Level 1 | 完整权限 | 部门列表 + 汇报线 + 所有人OKR | ⭐⭐⭐⭐⭐ 完整诊断 |
| Level 2 | 部分权限 | 部分人员OKR + 部分汇报线 | ⭐⭐⭐ 基础诊断 + 提示补全 |
| Level 3 | 最低权限 | 只能看到自己的OKR | ⭐⭐ 个人诊断 + 引导补充 |

### 自动降级

Skill 会自动检测权限级别，并在输出中说明：
- 权限不足时给出明确提示
- 建议如何申请更高权限
- 降级后仍能完成基础诊断

---

## 数据缓存

### 功能说明

为了提升重复诊断的效率，支持数据缓存机制。

### 使用方式

```bash
# 启用缓存（第一次会自动缓存）
python3 scripts/diagnose.py --department-name "团队名" --use-cache

# 强制刷新（忽略缓存，重新拉取）
python3 scripts/diagnose.py --department-name "团队名" --use-cache --force-refresh

# 自定义缓存目录
python3 scripts/diagnose.py --department-name "团队名" --use-cache --cache-dir ./my-cache

# 自定义缓存过期时间（1小时）
python3 scripts/diagnose.py --department-name "团队名" --use-cache --cache-ttl 3600
```

### 缓存规则

- 默认缓存 24 小时
- 缓存文件保存在 `./.cache/` 目录
- 按部门/用户生成不同的缓存键
- 自动检测缓存是否过期

---

## 目录结构

```
performance-goal-diagnosis/
├── SKILL.md                          # 主文档（本文件）
├── PRD.md                            # 产品需求文档
├── scripts/
│   ├── diagnose.py                   # 统一入口脚本（推荐）
│   ├── collect_data.py               # 数据采集脚本
│   ├── diagnose_goals.py             # 诊断核心脚本
│   ├── semantic_utils.py             # 语义分析模块（v2.0 新增）
│   ├── industry_matcher.py           # 行业指标匹配引擎（v2.2 新增 + v2.3 增强）
│   ├── indicator_tree.py              # 独立指标树模型/渲染器（v2.6 新增）
│   ├── tree_view.py                  # 层级强化版目标拆解树渲染器（v2.5 新增）
│   ├── import_template_output.py     # 模板技能输出→行业指标库桥接器（v2.5 新增）
│   ├── tests/
│   │   ├── test_indicator_tree.py          # 独立指标树契约测试（10 个用例）
│   │   ├── test_import_template_output.py  # 桥接器单元测试（43 个用例）
│   │   └── test_semantic_relationship.py   # 四层语义判断单元测试（48 个用例）
│   └── plugins/                      # 可选插件系统（v2.0 新增）
│       ├── __init__.py
│       ├── base_plugin.py            # 插件基类
│       ├── plugin_manager.py         # 插件管理器
│       ├── word_vector_plugin.py     # 词向量插件
│       ├── llm_plugin.py             # LLM 插件
│       └── feedback_plugin.py        # 反馈插件
├── references/
│   ├── diagnosis-rules.md            # 诊断规则详解
│   ├── data-collection.md            # 数据采集指南
│   ├── industry-metrics.md           # 行业指标库使用指南
│   ├── indicator-relationship-checklist.md  # 指标关联性判定 checklist
│   └── standard-output-framework.md  # v2.3 新增：标准输出框架规范
└── examples/
    ├── end-to-end-demo/              # 端到端完整示例
    │   ├── README.md
    │   ├── sample_input.json
    │   ├── sample_output.json
    │   ├── sample_report.md
    │   └── sample_report.html
    └── industry-metrics/             # 示例指标库（9大行业三层体系）
        ├── README.md
        ├── 数据质量与元数据标准.md     # v2.1 新增：元数据标准
        ├── 更新机制与流程.md           # v2.1 新增：更新机制
        ├── 检验标准与质量控制.md       # v2.1 新增：检验标准
        ├── 变更日志与版本管理.md       # v2.1 新增：版本管理
        ├── 制造业/
        ├── 消费品行业/
        ├── 医疗健康行业/
        ├── 汽车行业/
        ├── 电子半导体行业/
        ├── 通用行业/
        ├── 银行业/
        ├── 物流行业/
        ├── 新能源行业/
        ├── SaaS行业/                  # 早期版本
        ├── 互联网行业/                # 早期版本
        └── 通用/                      # 早期版本
```

---

## 典型使用场景

### 场景 1：季度初 OKR review

**痛点**：季度初设定 OKR 后，不知道拆解得合不合理，有没有问题。

**解决方案**：
```bash
python3 scripts/diagnose.py --department-name "你的部门" --format html
```

**产出**：HTML 可视化报告，列出所有问题和优化建议，直接在 review 会上用。

---

### 场景 2：新管理者上任诊断

**痛点**：刚接手一个团队，不了解团队的目标拆解情况，不知道从哪入手。

**解决方案**：
```bash
python3 scripts/diagnose.py --department-id od-xxxxx --format markdown
```

**产出**：Markdown 格式的诊断报告，快速了解团队目标拆解的健康度和主要问题。

---

### 场景 3：个人目标对齐检查

**痛点**：设定了个人 OKR，但不确定有没有对齐上级方向。

**解决方案**：
```bash
python3 scripts/diagnose.py --user-ids "ou_你的id" --format text
```

**产出**：检查你的目标是否都对齐到了上级目标，有没有遗漏。

---

### 场景 4：跨团队项目目标对齐

**痛点**：跨团队项目，多个团队的目标之间有没有对齐，责任有没有重叠或真空。

**解决方案**：
```bash
python3 scripts/diagnose.py --user-ids "ou_1,ou_2,ou_3" --format html
```

**产出**：识别目标重复、责任不清等问题，促进跨团队对齐。

---

### 场景 5：指标体系优化

**痛点**：团队的指标体系混乱，不知道哪些指标是真的有用，哪些是假拆解。

**解决方案**：
```bash
python3 scripts/diagnose.py --input team_metrics.json --format html --industry-metrics examples/industry-metrics
```

**产出**：13 种维度的指标诊断，识别假拆解、因果倒置、维度缺失等问题。

---

## 常见问题

### Q: 为什么有些同事的 OKR 看不到？

A: 这是权限限制。你只能看到有权限查看的 OKR 数据。Skill 会自动降级，并在结果中说明权限级别。

### Q: 诊断结果准确吗？

A: 诊断基于规则和启发式算法，v2.0 引入了语义分析，能发现大部分明显的问题，但不能替代人的判断。建议把诊断结果作为参考，结合实际情况判断。

如果需要更高的准确度，可以启用 LLM 插件。

### Q: 可以自定义诊断规则吗？

A: 当前版本支持通过修改 `diagnose_goals.py` 中的诊断逻辑来自定义。后续版本会增加更友好的自定义配置方式。

### Q: 数据会上传吗？

A: 不会。所有数据处理都在本地完成，不会上传到任何服务器。即使启用 LLM 插件，也只会发送指标名称等必要信息，不会发送完整的团队数据。

### Q: 支持哪些飞书数据？

A: 当前可执行路径是标准 JSON，或由用户提供真实接口地址、鉴权方式、分页和字段映射的飞书绩效 HTTP API 配置；适配器将人员、目标和指标响应标准化后进入同一诊断核心。历史部门/人员/“我的团队”快捷入口若缺少实际采集模块或权限，不应宣称已自动接通。

### Q: 语义分析需要安装额外依赖吗？

A: 核心语义分析功能（jieba 分词 + 同义词词典）只需要 jieba 库，可以用 `pip install jieba` 安装。

如果没有安装 jieba，会自动降级到简单 n-gram 分词，不影响使用。

### Q: 插件是必须的吗？

A: 不是。所有插件都是可选的。核心功能不需要任何插件就能正常运行。插件只是提供增强功能。

---

## 版本历史

## 版本历史

### v2.7（当前版本）

**重大更新：飞书绩效 API 适配 + 弱关系人工确认闭环 + 移动端关系详情**

- 新增 `scripts/feishu_performance_adapter.py`：通过用户提供的真实接口配置完成环境变量鉴权、分页、全量/逐人采集和字段标准化。
- 合并可选的飞书自建应用创建资源，但明确应用创建只负责凭证准备，不代表绩效接口或权限已开通。
- HTML 指标树新增“确认保留、拒绝关系、改选上级、待核实、导出关系确认 JSON、清空本次确认”。
- 新增 `--relation-decisions` 回读能力；显式关系不可被人工决策覆盖，确认/改选单独标记为 `user_confirmed`。
- 窄屏下指标详情改为底部浮层，支持关闭，核心操作保持可见和可点击。
- 报告框架升级为 6 页，指标树与目标树各自独立。
- 新增飞书绩效适配器和人工关系决策测试；全量 112 项测试通过。
- SaaS 样例完成浏览器导出 → CLI 回读验证：拒绝候选边后节点转为 `user_rejected` 数据缺口，算法不再恢复该边。

### v2.6

**重大更新：真正的独立指标拆解树 + 可审计关系来源**

- 新增 `scripts/indicator_tree.py`：指标具有稳定唯一 ID，并以独立节点进入树模型。
- 支持指标字段 `indicator_id` / `parent_indicator_ids` 及顶层 `indicator_relations` 显式关系。
- 显式关系使用实线和确定来源；没有显式关系时，只在父目标/对齐目标限定的候选范围内调用四层语义算法推断，并以虚线和置信度展示。
- 证据不足时保留指标节点，标记“数据缺口/未识别”，不强制建立父子边；同一目标内的多个指标不会自动互相连线。
- JSON 新增 `indicator_tree`，包含 `nodes`、`edges`、`root_indicator_ids`、`data_gaps`、上下关系索引和逐边四层诊断。
- HTML 新增独立“指标拆解树”页面并默认作为拆解视图，保留“目标拆解树”切换；点击指标查看向上关联、向下承接、来源、评分、风险和人工复核提示。
- Markdown 新增指标树摘要、逐边审计清单和数据缺口列表。
- 新增 10 个指标树契约测试；与原 91 个测试合计 101 个全部通过。
- 实际 SaaS 样例回归：296 个独立指标节点、70 条语义推断边、199 个关系数据缺口；由于样例缺少指标级 ID/父指标字段，显式边为 0，报告已明确提示数据质量限制。

### v2.5

**重大更新：层级强化版目标拆解树 + 四层语义关联判断法**

✅ **层级强化版目标拆解树（v2.5 核心）**：
- 新增 `scripts/tree_view.py` 渲染器，默认替换标准报告"目标拆解树"页面
- **部门分组卡片**：按部门彩色卡片分组，部门头含图标/名称/负责人/进度/问题数，可折叠
- **层级强化区分**：L2 蓝 / L3 紫 / L4 灰，彩色左缘条 + 层级徽标 + 缩进逐级递进（0/30/60px）
- **上下关联诊断面板**：点击任意节点，右侧面板展示 路径 / 指标 / ↑父级对齐 / ↓子级承接 / 自身结构问题
- **全部展开/收起**：一键折叠或展开所有部门
- 新增 `--tree-style` 参数：`enhanced`（默认，v2.5 层级强化版）/ `legacy`（v2.4 按层级分组）
- 增强视图生成失败时自动回退到 legacy，不影响报告输出

✅ **四层语义关联判断法（v2.5 核心）**：
- 在 `semantic_utils.py` 新增 `assess_relationship()` 方法，封装由粗到细的四层判定
- **第1层 词面相似**：字符重叠 + 包含关系检测（识别"岗位前缀复制"假拆解）
- **第2层 语义相似**：jieba 分词 + 同义词映射（识别"换说法"假拆解）
- **第3层 因果方向**：过程(因) → 结果(果) 方向判定（识别因果倒置）
- **第4层 数值链路**：下级目标值加总 vs 上级目标值（识别数值不闭合）
- 综合计分 + 硬信号压级（因果倒置封顶0.2 / 假拆解封顶0.35），输出 strong/medium/weak/none 四级
- 判定边界文档化：语义关联是"弱信号"需人工复核、跨域关联例外、无数值时跳过数值层

✅ **模板→诊断闭环桥接（v2.5 新增）**：
- 新增 `scripts/import_template_output.py` 桥接器
- 新增 `--import-template` 参数：传入 industry-performance-template 技能输出的绩效方案 Markdown（第4章指标表），自动解析指标并按岗位分层（战略/管理/执行），生成诊断技能可消费的三层行业指标库
- 完成用户工作流闭环：先应用模板技能产出行业绩效模板 → 再应用本技能对目标拆解对标诊断

✅ **自动化单元测试（v2.5 新增）**：
- `scripts/tests/test_import_template_output.py`：桥接器 43 个 pytest 用例，覆盖表格解析/数值提取/岗位分层/字段映射/文件格式/端到端
- `scripts/tests/test_semantic_relationship.py`：语义判断 48 个 pytest 用例，覆盖同义词、反向指标、因果方向、假拆解、语义重叠、数值边界、硬信号压级、等级阈值、空值容错与返回契约
- 全量运行：`python3 -m pytest scripts/tests/ -v`（共 91 个用例）
- 测试驱动发现并修复 4 个真实 bug：①"部门负责人"被误归战略层；②"关键岗位"列未识别导致关联地图表指标错分层；③综合语义评估未正确消费因果函数的字符串返回值，因果方向被统一判为 unknown；④空指标文本传入语义评估时抛出 TypeError

✅ **文档更新**：
- `references/indicator-relationship-checklist.md` 新增"九、四层语义关联判断法"章节
- `references/standard-output-framework.md` 同步层级强化版树视图规范

### v2.4

**重大更新：标准输出框架默认化 + 目标树诊断融合 + 行业匹配算法优化**

✅ **标准输出框架设为默认（v2.4 核心）**：
- 5 页面标准化输出（概览/拆解树/诊断/建议/人员）成为默认输出格式
- 新增 `--legacy-format` 参数，可降级到旧版输出格式
- 参考「绩效目标拆解诊断系统」飞书应用的 UI 和内容维度
- 左侧固定导航栏 + 顶部栏 + 5 个 Tab 页面切换
- 响应式设计，支持移动端适配

✅ **目标拆解树融入诊断结果和优化建议（v2.4 核心）**：
- HTML 版：每个目标节点可点击展开，显示指标列表、诊断问题、优化建议、子目标
- 目标节点头部显示问题徽章（🔴高/🟡中/🔵低/✅健康）、进度条、负责人、层级
- 支持「全部展开/全部收起」操作
- 节点状态颜色：健康绿/警告黄/危险红（根据关联问题的最高严重度）
- Markdown 版：每个目标下显示关联的问题列表和优化建议
- 问题按严重度排序，关联的建议紧跟问题显示

✅ **行业匹配算法大幅优化（v2.4 核心）**：
- **行业识别优化**：
  - 制造业关键词从 20 个扩充到 45 个（新增：制造集团、智能制造、工业4.0、MES、班组、工段等）
  - 新增泛化行业降权机制（通用行业权重降低到 0.2），避免"集团""综合"等词误判
  - "制造集团"等混合名称现在能正确识别为制造业（置信度 1.0）
- **指标匹配算法重构**：
  - 新增 34 组指标同义词映射（OEE=设备综合效率、稼动率=设备利用率、一次合格率=直通率等）
  - 新增综合相似度算法：完全匹配→同义词匹配→包含关系匹配→Jaccard基础相似度→同义词重叠检测
  - 支持跨层级相邻匹配（战略层↔管理层↔执行层，相邻层级 +0.05 加成）
  - 匹配阈值从 0.5 降低到 0.4，候选筛选阈值从 0.3 降低到 0.25
  - 匹配效果：制造业示例 15 个指标中 12 个正确匹配（相似度 1.0），行业基准诊断问题从 1 个增加到 11 个

✅ **Bug 修复**：
- 修复 `html.escape(None)` 报错问题（改用 `or` 运算符处理空值）
- 修复目标拆解树中 `is_root_level` 未定义的 Bug
- 修复标准报告生成器中 `industry_info` 读取路径错误（从 `result.summary.industry_info` 读取）

✅ **版本号统一**：所有脚本版本号统一更新为 v2.4，行业指标库版本 v2.4.0

### v2.3

**重大更新：行业指标智能匹配引擎 + 自定义扩展 + 标准输出框架**

✅ **行业指标智能匹配引擎（v2.2 核心）**：
- 新增 `industry_matcher.py` 核心模块，封装5大能力
- **行业自动识别**：从部门名称、企业名称、目标内容多源识别，返回Top3候选+置信度
- **分层指标匹配**：行业→层级→名称语义三层匹配，层级和行业匹配有加成
- **指标类型判断**：自动识别正向指标（越高越好）、反向指标（越低越好）、区间指标（范围内最好）
- **智能偏离判断**：根据指标类型智能判断是否偏离（正向指标低于基准才算问题，反向指标高于基准才算问题）
- **匹配质量评估**：综合匹配相似度、数据源权威性、数据时效性、验证状态，输出A/B/C/D可信度等级
- **默认自动加载**：内置指标库默认自动加载，无需手动指定路径
- **报告展示增强**：诊断报告中展示行业匹配信息、元数据、匹配质量

✅ **自定义指标库合并（v2.3 新增）**：
- 支持合并用户自定义的行业指标库
- 支持单个文件或整个目录
- 支持 .md 和 .json 格式
- 可选择是否覆盖同名指标
- 自定义指标标记 `is_custom=True`，便于区分

✅ **用户反馈自动优化（v2.3 新增）**：
- 支持记录用户对指标的反馈（基准值不对、范围不对、指标未找到等）
- 同一指标同一类型反馈 ≥3 条自动应用到指标库
- 支持反馈统计和查询
- 被反馈更新的指标标记 `feedback_updated=True`

✅ **版本兼容性检查（v2.3 新增）**：
- 指标库版本号管理（当前 v2.3.0）
- 支持最低版本要求检查
- 返回支持的功能列表

✅ **新命令行参数**：
- `--industry`：手动指定行业（如：制造业、汽车行业、新能源行业），不指定则自动识别
- `--no-industry-metrics`：禁用行业指标库（不加载内置指标库）

✅ **输出增强**：
- JSON 输出新增 `industry_info` 字段：用户指定行业、自动识别行业、Top3候选、置信度、指标库统计
- 文本报告新增"🏭 行业基准匹配信息"区块
- 行业基准诊断问题展示完整元数据：数据来源、数据截止时间、验证状态、合理范围、匹配质量

✅ **标准输出框架（v2.3 新增）**：
- 参考「绩效目标拆解诊断系统」飞书应用，统一 5 页面标准化输出
- 新增 `references/standard-output-framework.md` 规范文档
- 5 大页面：概览 Dashboard、目标拆解树、诊断结果、优化建议、人员视图
- 统一 UI 设计规范：颜色体系、布局规范、组件样式
- HTML 报告采用左侧导航 + Tab 切换 5 页面结构
- Markdown 报告按 5 页面分章节组织

### v2.2

**核心更新：行业指标智能匹配引擎**

（已合并到 v2.3，详见上方）

### v2.1

**重大更新：数据质量提升版**

✅ **行业指标库数据质量体系**：
- 新增 4 个管理文档：数据质量与元数据标准、更新机制与流程、检验标准与质量控制、变更日志与版本管理
- 为所有战略层指标补充元数据：发布机构、数据截止时间、来源链接、稳定性分级、验证状态
- 为所有管理层/执行层指标补充元数据：数据来源、数据截止时间、稳定性分级
- 新增数据质量声明和使用说明
- 建立指标稳定性分级：稳定（年更）/ 较稳定（半年更）/ 动态（季更）
- 建立数据源权威性五级分级（⭐到⭐⭐⭐⭐⭐）
- 建立偏差预警机制（<10%正常、10-20%黄色、20-50%橙色、>50%红色）
- 建立质量评分体系（5维度加权评分）

✅ **用户反馈入口**：
- 在所有报告格式（text/markdown/html）末尾添加用户反馈快捷入口
- 支持反馈"指标不准"或"指标不对"
- 同一指标收到 ≥3 个类似反馈将触发即时更新
- 响应时间：3个工作日内

✅ **更新机制与版本管理**：
- 建立定期更新机制：每年1月全量复核、4月/7月/10月季度更新
- 建立触发式更新机制：用户反馈、重大标准发布、行业重大变化
- 建立版本号规则：主版本.次版本.修订号
- 完整的变更日志记录

### v2.0

**重大更新：语义增强版**

✅ **新增语义分析引擎**：
- jieba 中文分词集成
- HR/绩效领域同义词词典（200+ 词对）
- 多维度相似度融合（字符+词语+结构）
- TF-IDF 关键词权重
- 指标类型分类（10 大类型）
- 因果方向判断
- 否定词/反向词处理

✅ **新增 9 种检测模式**（从 4 种增加到 13 种）：
- 因果方向检测
- 粒度一致性检测
- 维度完整性检测（4 大管理框架）
- 逻辑闭环检测
- 命名质量检测
- 子指标重叠检测（增强版）
- 数学关系检测
- 拆解深度检测
- 横向目标对齐检测

✅ **新增可选插件系统**：
- 词向量相似度插件
- LLM 语义理解插件
- 主动学习反馈插件
- 插件管理器和标准接口

✅ **其他优化**：
- 假拆解检测准确度提升（从字符相似度升级为语义相似度）
- 关联性检测准确度提升（多维度融合 + 同义词）
- 向后兼容，没有 jieba 时自动降级

### v1.5

- ✅ 新增第八大诊断维度：指标关联性与承接关系
- ✅ 新增 4 种自动检测模式（关键词关联、假拆解、数量合理性、承接强弱）
- ✅ 新增数据质量检查（5 项检查）
- ✅ 新增智能容错机制
- ✅ 新增指标关联性判定 checklist
- ✅ 完善异常处理和降级机制

### v1.4

- ✅ 新增统一入口脚本 `diagnose.py`，一站式完成数据采集 + 诊断 + 输出
- ✅ 新增 Markdown 输出格式，可直接贴到飞书文档
- ✅ 新增 HTML 可视化报告，可直接在浏览器打开
- ✅ 新增数据缓存机制，重复诊断更快
- ✅ 新增端到端完整示例
- ✅ 新增输入数据自动校验

### v1.3

- ✅ 新增自动数据采集模块（通讯录 API + OKR 批量获取）
- ✅ 新增三级权限降级机制
- ✅ 支持 4 种输入方式（部门 ID/部门名称/用户列表/我的团队）
- ✅ 新增数据采集指南文档

### v1.2

- ✅ 新增第七大诊断维度：指标偏离行业基准
- ✅ 支持关联本地文件夹作为行业指标库
- ✅ 支持 Markdown 和 JSON 两种指标格式
- ✅ 新增行业指标库使用指南
- ✅ 新增示例指标库（互联网 + SaaS + 通用）

### v1.1

- ✅ 支持飞书绩效指标详情 API
- ✅ 完善数据格式定义
- ✅ 新增 PRD 文档

### v1.0

- ✅ 基础版本：六大诊断维度
- ✅ 目标拆解树构建
- ✅ 健康度评分
- ✅ 优化建议生成
- ✅ 基础文档

---

## 依赖说明

### 核心依赖（必须）
- Python 3.7+
- 标准库即可运行核心功能

### 推荐依赖（建议安装）
- jieba：中文分词（`pip install jieba`）
  - 不安装也能运行，会自动降级到简单分词

### 可选插件依赖
- numpy：词向量插件需要
- openai：LLM 插件需要
- 词向量文件：词向量插件需要

### 数据采集依赖
- 飞书 CLI（lark-cli）- 用于自动采集数据

---

## 相关文档

- [诊断规则详解](references/diagnosis-rules.md) - 八大诊断维度的详细规则说明
- [数据采集指南](references/data-collection.md) - 数据采集模块的详细说明
- [行业指标库使用指南](references/industry-metrics.md) - 行业指标库的使用和自定义
- [指标关联性判定 Checklist](references/indicator-relationship-checklist.md) - 指标关联性与承接关系的判定标准
- [标准输出框架规范](references/standard-output-framework.md) - v2.3 新增：5 页面标准化输出框架详细规范
- [产品需求文档](PRD.md) - 完整的产品需求说明
